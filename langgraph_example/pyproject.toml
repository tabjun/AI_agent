[project]
name = "langchain-example"
version = "0.1.0"
description = "LangChain & LangGraph implementation examples"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain-openai",
    "langgraph",
    "python-dotenv",
]
